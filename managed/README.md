# managed/

This directory is **auto-generated** by the Compact compiler.

Run this command to populate it:
```bash
compact compile contracts/shield_cert.compact --output managed
```

After compilation you will see:
- `*.zk` — compiled circuit files
- `*.ts` — TypeScript type bindings for your DApp
- Proving and verification keys

**Do not manually edit files in this directory.**
